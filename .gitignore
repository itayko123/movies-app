# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

node_modules/
.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` (Continuous Native Generation)
ios/
android/

# Secrets — never commit real keys. `.env.example` must contain placeholders only.
.env
.env.*.local
supabase/.env.local

# Firebase service files contain project identifiers — keep them out of git
google-services.json
GoogleService-Info.plist

# Signing material
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision

# Misc
.metro-health-check*
npm-debug.*
.DS_Store
*.orig.*
*.tsbuildinfo

.vercel

# Supabase CLI scratch state — contains start-secrets/**/docker.env with the
# service-role key, DB URL and JWT secret. Never commit.
supabase/.temp/

# Native export output — build artifact, not source.
dist-ios/

# Reference screenshots — large binaries, not needed to build.
new app screenshots/

# Separate git repo vendored in by hand; not a submodule.
screenshot-to-code/
